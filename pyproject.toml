[project]
name = "skin-analyzer"
version = "1.0.0"
requires-python = ">= 3.12"
dependencies = [
    "flask",
    "flask-cors",
    "python-dotenv",
    "numpy",
    "Pillow",
    "ai-edge-litert",
]

[tool.vercel]
entrypoint = "backend.src.app:app"