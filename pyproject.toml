[project]
name = "skincare-analyzer"
version = "0.1.0"
requires-python = ">=3.10"
dependencies = [
    "flask",
    "flask-cors",
    "python-dotenv",
    "pillow",
    "numpy",
]

[tool.vercel]
entrypoint = "backend.src.app:app"